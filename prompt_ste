Parfait, on va faire un **ordre de prompts optimisé pour Claude** pour que :

* tu ne casses pas la cohérence entre backend / frontend / DB
* la base corresponde exactement aux entities
* Codex puisse brancher sans friction

👉 **Réponse courte à ta question :**
🟢 Le prompt **BASE DE DONNÉES → tu le donnes APRÈS que Claude ait généré les ENTITIES BACKEND**
(car la DB doit refléter exactement les entities JPA).

---

# 🧠 ORDRE LOGIQUE DES PROMPTS POUR CLAUDE

## 🟩 ÉTAPE 1 — Architecture globale (toujours en premier)

```text
Tu es un lead dev fullstack. Je dois livrer un MVP en 5 jours.

Projet : HealDrive (transport médical connecté)

Stack imposée :
- Backend : Spring Boot 3 (Java), API REST
- Frontend : React + Tailwind
- DB : PostgreSQL
- POO : héritage + polymorphisme obligatoires

Fonction principale :
prise de rendez-vous médical → demande de transport → smart matching → suivi → chat → anti-retard → paiement à la fin (non prioritaire).

Fonctions :
- Auth + rôles : PATIENT, AIDANT, CHAUFFEUR, ADMIN
- CRUD RendezVousMedical
- Demande transport liée à un RDV
- Matching transporteur dispo dans 5 km compatible besoins
- Suivi statut + ETA
- Chat
- Réassignation automatique si problème

Donne :
1) architecture backend
2) architecture frontend
3) arborescence projet complète
4) flux métier
5) choix techniques (héritage JPA, stratégie)
NE CODE PAS.
```

---

## 🟩 ÉTAPE 2 — Backend ENTITIES + polymorphisme

```text
Génère les entities JPA complètes pour HealDrive.

Contraintes :
- UUID
- Lombok
- Héritage Transporteur (SINGLE_TABLE)
- Enum pour les statuts
- Relations propres

Entités :
Utilisateur
Transporteur (abstract) → TaxiConventionne, VSL, Ambulance
RendezVousMedical
Trajet
Message
Paiement

Ajoute :
- calculerPrix() polymorphe
- validations Bean Validation

Donne uniquement le code des entities.
```

---

## 🟩 ÉTAPE 3 — 🔥 BASE DE DONNÉES SQL (MAINTENANT)

👉 Tu le donnes ici parce que les entities existent.

```text
Génère un fichier SQL PostgreSQL UNIQUE basé EXACTEMENT sur les entities JPA précédentes.

Objectif :
psql -U postgres -d healdrive -f healdrive.sql

Contenu :
- DROP TABLE dans le bon ordre
- CREATE EXTENSION uuid-ossp
- ENUM
- TABLES
- FK
- INDEX
- DONNÉES DE TEST
- Script idempotent

ddl-auto devra être en validate côté Spring.

Donne le fichier healdrive.sql complet.
```

---

## 🟩 ÉTAPE 4 — Repositories

```text
Génère tous les Repository Spring Data JPA.

Ajoute :
méthode pour récupérer les transporteurs disponibles dans un rayon de 5km
(bounding box).
```

---

## 🟩 ÉTAPE 5 — Services (logique métier)

```text
Génère la couche Service complète avec :

- création RDV
- création trajet
- smart matching automatique
- calcul ETA (simulé)
- anti-retard
- réassignation
- chat

Architecture propre + DTO.
```

---

## 🟩 ÉTAPE 6 — Controllers REST

```text
Génère tous les controllers REST.

Routes :
/auth
/rdv
/trajets
/transporteurs
/messages

DTO + gestion d’erreurs globale.
```

---

## 🟩 ÉTAPE 7 — Configuration Spring Boot

```text
Génère :

application.yml
config PostgreSQL
config validation ddl-auto=validate
config CORS pour React
data.sql vide

+ README lancement projet.
```

---

# 🟦 FRONTEND

## 🟦 ÉTAPE 8 — Structure React

```text
Génère la structure React + Tailwind + Vite.

Pages :
login
dashboard patient
création RDV
demande transport
suivi trajet + chat
dashboard chauffeur

API mock.
```

---

## 🟦 ÉTAPE 9 — Auth + routing par rôle

```text
Ajoute :
AuthContext
routes protégées
redirection par rôle
fake backend api.ts
```

---

## 🟦 ÉTAPE 10 — Suivi live + anti-retard UI

```text
Ajoute simulation ETA + timeline statuts + alerte retard.
```

---

# 🧩 POUR CODEX (APRÈS CLAUDE)

Quand tout est généré :

```text
Relie le frontend React à l’API Spring Boot.
Remplace les mocks par des appels HTTP.
Mappe les DTO.
Ajoute loaders + gestion erreurs.
```

---

# ⭐ RÉSUMÉ SIMPLE DE L’ORDRE

1️⃣ Architecture
2️⃣ Entities
3️⃣ 🔥 Base SQL
4️⃣ Repositories
5️⃣ Services
6️⃣ Controllers
7️⃣ Config Spring
8️⃣ Frontend structure
9️⃣ Auth + routing
🔟 Suivi live UI

---

# 🚀 CONSEIL POUR QUE CLAUDE SOIT ULTRA EFFICACE

Avant CHAQUE prompt ajoute :

```text
On continue le même projet HealDrive.
Ne régénère pas ce qui existe déjà.
Respecte exactement les choix précédents.
```

---

# 🎁 Si tu veux

Je peux te faire :

✅ la version **copiable en 1 seule fois**
✅ ou la version **checklist “jour 1 → jour 5” pour ton dev réel**
✅ ou la version **Claude 100% optimisée pour éviter qu’il parte dans tous les sens**.

Tu veux qu’on passe en mode **plan de production sur 5 jours** maintenant ?
